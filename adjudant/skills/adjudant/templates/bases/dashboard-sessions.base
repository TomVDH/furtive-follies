filters:
  and:
    - file.inFolder("projects/{slug}/sessions")
    - 'type == "session"'
views:
  - type: table
    name: Sessions, newest first
    sort:
      - property: file.name
        direction: DESC
    order:
      - file.name
      - created
      - updated
